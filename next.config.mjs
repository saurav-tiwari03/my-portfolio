/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "api.microlink.io", 
      'avatar.vercel.sh',
      'x.com',
      'res.cloudinary.com'
    ],
  },
};

export default nextConfig;
